# 更新日志（Changelog）

本项目所有值得记录的改动都写在这里。日期用 YYYY-MM-DD。

## 2026-07-24

### 新增
- **控制面板参数决策法（三层）**：把"生成哪些控件"从模型临场发挥，改为可预测的规则，解决不同模型/不同次生成参数数量飘忽、跨 Agent 不一致的问题。
  - 第一层·结构必有：文案框、每张图换图能力、每个颜色取色器、语言预设、四个出图按钮、固定画布尺寸。
  - 第二层·元素类型配方表：按「文字 / 图片 / 线条形状 / 图标标记 / 质感背景」五类，各配一套固定控件；遇到没见过的新元素也能先归类再套配方。
  - 交付前自查清单：第一层是否全在、第二层是否和图里元素一一对上、有无多余控件。
- **中/英语言排版预设固定为必有项**：不论用户给中文还是英文文案，面板都带「中文预设 / 英文预设」切换；切中文时自动切字体降级链并放松行高与顶距，"换语言不散版"。（此前是条件功能，导致不同 Agent 有时做有时不做。）

### 变更
- **目标尺寸不再默认小红书**：动手前必须先问用户发哪个平台（小红书 / 公众号 / B站 / 方图 / 视频…），只是把这一问和"问文案"合并成同一句，不额外增加提问轮次。
- README 使用流程与 SKILL 描述同步以上两点。
